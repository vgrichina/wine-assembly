#!/usr/bin/env node
// Compatibility entry point. Transparent and disabled blits are WAT-owned;
// the canonical raster-handler suite contains their exact color-key tests.

'use strict';

require('./test-wat-gdi-raster-handlers');
